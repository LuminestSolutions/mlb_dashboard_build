# Deprecated Import Folder

Manual CSV uploads are no longer part of the active dashboard workflow.

The dashboard now pulls Baseball Savant / Statcast data through `pybaseball` during refresh and stores team metrics in SQLite.
