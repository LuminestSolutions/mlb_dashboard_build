# Copy this file to .streamlit/secrets.toml for local secrets.
# Do not commit real API keys.

MLB_EDGE_DB_PATH = "data/mlb_edge.db"
PYBASEBALL_CACHE = "data/pybaseball_cache"
MPLCONFIGDIR = "data/matplotlib_config"

# Optional. MLB Stats schedule works publicly; set only if you have a marker/key to pass.
# MLB_STATS_API_MARKER = "your-mlb-stats-marker"

# Add this only after creating The Odds API login/key.
# ODDS_API_KEY = "your-odds-api-key"

DASHBOARD_PASSWORD = ""
